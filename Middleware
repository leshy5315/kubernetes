apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: block-invalid-host
  namespace: ssl-web
spec:
  redirectRegex:
    regex: ^https?://.*$   # ловим все запросы
    replacement: http://0.0.0.0 # несуществующий адрес
    permanent: false

