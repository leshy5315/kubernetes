apiVersion: v1
kind: Service
metadata:
  name: myapp
  labels:
    env  : prod
    owner: Lesyk
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: nginx          # Selecting PODS those Labels