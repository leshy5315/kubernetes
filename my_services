apiVersion: v1
kind: Service
metadata:
  name: nginx-autoscale
  labels:
    env  : prod
    owner: Lesyk
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8888
  selector:
    app: nginx          # Selecting PODS those Labels
