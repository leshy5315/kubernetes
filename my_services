apiVersion: v1
kind: Service
metadata:
  name: myapp
  labels:
    env  : prod
    owner: Lesyk
spec:
  type: ClusterIP
  ports:
    - targetPort: 80
      port: 80
  selector:
    type: ClusterIP
    app: nginx          # Selecting PODS those Labels
